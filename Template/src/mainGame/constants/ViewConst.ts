/**
 * 场景常量   SceneConst[key]取其key的值
 * @author lucywang
 * @date 2018/01/05
 */
enum SceneConst {
    LoginScene,
    HallScene,
    GameScene,
} 

enum PopUpLayers {
    ShopLayer,
    RulelLayer,
    SettingLayer,
}